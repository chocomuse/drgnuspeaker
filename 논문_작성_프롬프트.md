# Jetson Nano AI 스피커 논문 작성 프롬프트

아래 프롬프트를 논문 작성 AI에 그대로 입력하고, 대괄호로 표시된 항목만 실제 정보로 바꾸어 사용한다.

---

## 프롬프트

당신은 임베디드 시스템, 엣지 컴퓨팅, 음성 인터페이스 및 IoT 분야의 연구 논문 작성 전문가다. 아래에 제공하는 실제 구현 정보를 바탕으로 한국어 학술 논문 초안을 작성하라. 구현되지 않은 기능이나 측정하지 않은 결과를 임의로 만들어내지 말고, 미측정 항목은 반드시 "추후 실험 필요" 또는 "측정값 입력 필요"로 표시하라.

### 1. 연구 대상

본 연구의 대상은 Jetson Nano를 중심으로 구현한 화면 없는 AI 스피커 시스템이다. 이 시스템은 다음 목적을 가진다.

1. 디스플레이와 키보드 없이 초기 Wi-Fi를 설정한다.
2. Android 앱과 로컬 네트워크에서 스피커를 탐색하고 사용자 계정에 페어링한다.
3. Google Speech-to-Text를 이용하여 호출어와 사용자 질문을 인식한다.
4. 녹음 음성과 STT 결과를 백엔드 분석 서버로 전송한다.
5. 서버의 답변, 상태 점수 및 위험 수준을 음성으로 출력한다.
6. 앱에서 마이크 음소거, TTS 활성화, 녹음 시간 및 음성 프로필 등의 설정을 관리한다.
7. systemd 서비스를 통해 Jetson Nano 부팅 후 자동 실행한다.

### 2. 하드웨어 구성

논문에서는 다음 하드웨어를 시스템 구성요소로 설명하라.

- 연산 장치: NVIDIA Jetson Nano
- 음성 입력: USB 마이크 또는 USB 마이크 어레이
- 음성 출력: USB, 3.5 mm, HDMI 또는 Bluetooth 스피커
- 네트워크: Jetson Nano의 Wi-Fi 인터페이스 또는 USB Wi-Fi 동글
- 전원: [사용 전원 어댑터 규격 입력]
- 냉각: [방열판 및 냉각팬 정보 입력]
- 선택 장치: GPIO 버튼, 상태 LED

실제 실험에 사용하지 않은 하드웨어는 구현 완료로 표현하지 말고 선택 또는 확장 가능한 구성으로 구분하라.

### 3. 소프트웨어 모듈

다음 Python 모듈과 역할을 표로 정리하고 본문에서 설명하라.

- `main.py`: 전체 실행 순서와 상태 전환 관리
- `config.py`: `.env` 기반 장치, 서버, STT, Wi-Fi 및 페어링 설정 로드
- `wifi_provisioner.py`: 인터넷 확인, 임시 AP 생성, Wi-Fi 설정 HTTP API 및 NetworkManager 제어
- `local_pairing.py`: mDNS 광고와 `/pair`, `/settings`, `/unlink` 로컬 API 제공
- `pairing.py`: 백엔드 페어링 요청, 장치 토큰 및 사용자 정보 저장·삭제
- `wake_word.py`: 키보드, Vosk 또는 Google STT 기반 호출어 감지
- `stt.py`: Google Speech-to-Text REST API 호출 및 서비스 계정 OAuth 인증
- `audio_io.py`: 마이크 녹음, WAV 저장 및 무음 감지
- `api_client.py`: 백엔드 `/api/stt-analyze` 요청과 응답 파싱
- `settings_sync.py`: 백엔드 장치 설정 주기적 동기화
- `runtime_settings.py`: 실행 중인 스피커 설정의 스레드 안전 관리
- `tts.py`: 외부 TTS 명령을 이용한 음성 출력
- `status.py`: idle, listening, recording, thinking, speaking, error 상태 관리

### 4. 시스템 작동 절차

다음 순서를 기반으로 전체 동작 파이프라인을 설명하라.

1. Jetson Nano가 부팅되면 systemd가 스피커 프로그램을 실행한다.
2. 프로그램은 `.env`에서 서버 주소, API 키, 장치 ID, Google 서비스 계정 경로 및 오디오 설정을 읽는다.
3. 인터넷 연결을 확인한다.
4. 인터넷이 없으면 `DrGNU-Speaker-Setup` 임시 AP를 생성한다.
5. AP의 기본 게이트웨이는 `192.168.4.1`, 설정 서버 포트는 `8765`이다.
6. Android 앱이 AP에 접속하고 `POST /wifi`로 공유기 SSID와 비밀번호를 전송한다.
7. Jetson은 `nmcli`를 사용하여 대상 Wi-Fi 접속을 시도한다.
8. 앱은 `GET /status`를 호출하여 연결 상태를 확인한다.
9. 연결 성공 시 임시 AP와 설정 서버를 종료한다.
10. 실제 LAN IP에서 mDNS와 로컬 페어링 서버를 시작한다.
11. 앱이 `POST /pair`를 호출하면 Jetson은 백엔드 `POST /api/devices/link-local`을 호출한다.
12. 페어링 성공 시 `.device-token`과 `.user-info`를 로컬에 저장한다.
13. 스피커는 호출어를 대기한다.
14. 현재 Google STT 호출어 모드에서는 짧은 오디오 구간을 녹음하여 Google STT로 변환하고, `지누야`, `진우야` 등의 등록된 호출어와 비교한다.
15. 호출어가 감지되면 준비 음성을 출력하고 사용자 질문을 녹음한다.
16. 질문 WAV 파일을 Google STT로 변환한다.
17. 원본 WAV 파일과 `local_stt`, `stt`, `transcript` 텍스트를 백엔드 `/api/stt-analyze`로 전송한다.
18. 서버의 답변, 점수, 위험 수준을 파싱하여 TTS로 출력한다.
19. 앱에서 연결 해제 시 `POST /unlink`를 호출하고 Jetson은 `.device-token`과 `.user-info`를 삭제한다.

### 5. 통신 구조

다음 통신 관계를 표로 작성하라. 각 항목에는 송신자, 수신자, 프로토콜, 엔드포인트, 데이터 및 목적을 포함하라.

#### 초기 Wi-Fi 설정

- 앱 → Jetson: `POST http://192.168.4.1:8765/wifi`
- JSON 필드: `ssid`, `password`, `device_id`, `user_id`
- 앱 → Jetson: `GET http://192.168.4.1:8765/status`
- 통신 방식: 설정 AP 내부의 평문 HTTP

#### 로컬 페어링

- Jetson → LAN: `_drgnu-speaker._tcp.local.` mDNS 광고
- 앱 → Jetson: `POST http://{speaker-ip}:8765/pair`
- 앱 → Jetson: `GET http://{speaker-ip}:8765/settings`
- 앱 → Jetson: `POST http://{speaker-ip}:8765/unlink`

#### 백엔드 통신

- Jetson → 서버: `POST {base_url}/api/devices/link-local`
- Jetson → 서버: `GET {base_url}/api/devices/{device_id}/settings`
- Jetson → 서버: `POST {base_url}/api/stt-analyze`
- 인증: `X-API-Key`, `X-Device-Id`, 페어링 후 `Authorization: Bearer {device_access_token}`

#### Google STT 통신

- Jetson → Google: `POST https://speech.googleapis.com/v1/speech:recognize`
- 인증: Google Cloud 서비스 계정으로 발급한 OAuth Bearer 토큰 또는 API 키
- 오디오: 16-bit LINEAR16 WAV, 기본 16 kHz, 모노
- 언어: `ko-KR`

### 6. 보안 및 개인정보

다음 내용을 별도 절로 분석하라.

- `.env`, `.device-token`, `.user-info`, Google 서비스 계정 JSON은 Git 저장소에서 제외한다.
- 초기 AP의 HTTP 통신에는 공유기 비밀번호가 포함되므로 AP 접근 제어와 제한된 설정 시간을 고려해야 한다.
- Android에서는 로컬 HTTP 통신을 허용하는 네트워크 보안 설정이 필요하다.
- 장치 인증에는 서버 API 키와 장치 액세스 토큰을 사용한다.
- 앱 연결 해제 시 로컬 토큰과 사용자 정보를 삭제한다.
- 음성 데이터가 Google 및 백엔드로 전송되므로 사용자 동의, 보관 기간, 암호화 및 비식별화 정책을 논의해야 한다.
- 현재 구현의 보안 한계와 개선안으로 HTTPS, 요청 서명, AP 비밀번호 무작위화, 설정 API 만료 시간 및 재설정 버튼을 제안하라.

### 7. 연구 질문

다음 연구 질문을 논문의 중심 질문으로 다듬어 제시하라.

- RQ1: 화면 없는 Jetson Nano AI 스피커에서 AP 기반 모바일 Wi-Fi 설정은 초기 설치 편의성과 성공률을 얼마나 개선하는가?
- RQ2: Google STT 기반 호출어 감지는 로컬 Vosk 방식보다 오탐률과 미탐률을 얼마나 줄이는가?
- RQ3: 네트워크 상태, 사용자 거리 및 주변 소음이 호출어 감지율과 전체 응답 지연시간에 어떤 영향을 미치는가?
- RQ4: 엣지 장치와 클라우드 서비스를 결합한 구조가 정확도, 지연시간, 전력 소비 및 개인정보 측면에서 어떤 상충관계를 가지는가?

### 8. 실험 설계

측정하지 않은 값을 만들지 말고 아래 실험 계획과 빈 결과표를 제안하라.

1. 호출어 정확도
   - 정상 호출 성공률, 미탐률, 오탐률
   - 호출어: 지누야, 진우야, 유사어, 무의미 발화
   - 거리: 0.5 m, 1 m, 2 m, 3 m
   - 소음: 조용한 환경, 40 dB, 60 dB, 80 dB
   - 비교: Vosk 방식과 Google STT 방식

2. 질문 STT 정확도
   - 한국어 문장 인식률 또는 WER
   - 짧은 문장, 긴 문장, 고유명사, 소음 포함 문장

3. 응답 지연시간
   - 호출어 발화부터 감지까지
   - 질문 종료부터 STT 완료까지
   - 백엔드 요청부터 응답까지
   - 전체 음성 응답 시작까지

4. 초기 Wi-Fi 설정
   - AP 생성 시간
   - 앱 연결 성공률
   - Wi-Fi 설정 완료 시간
   - 잘못된 비밀번호 입력 후 복구 성공률

5. 하드웨어 성능
   - CPU 사용률, 메모리 사용량, 온도
   - 대기·녹음·STT 요청·TTS 출력 단계별 소비전력
   - 8시간 또는 24시간 연속 실행 안정성

각 조건은 가능하면 30회 이상 반복하고 평균, 표준편차, 중앙값 및 95% 신뢰구간을 제시하도록 설계하라.

### 9. 논문 구성

다음 목차로 작성하라.

1. 제목
2. 국문 초록
3. 영문 Abstract
4. 서론
5. 관련 연구
6. 시스템 요구사항
7. 하드웨어 및 소프트웨어 설계
8. 통신 프로토콜과 작동 파이프라인
9. 구현
10. 실험 환경 및 평가 방법
11. 실험 결과
12. 논의
13. 보안, 개인정보 및 한계
14. 결론 및 향후 연구
15. 참고문헌 후보

### 10. 작성 규칙

- 학술적이고 객관적인 문체를 사용한다.
- 구현 사실, 설계 의도, 실험 결과를 구분한다.
- 소스 코드에 존재하지 않는 기능은 구현된 것처럼 쓰지 않는다.
- 측정값을 임의 생성하지 않는다.
- 결과가 없는 표는 `측정값 입력 필요`로 표시한다.
- 표에는 하드웨어 사양, 소프트웨어 모듈, API, 실험 조건 및 결과를 포함한다.
- 그림으로 시스템 구성도, 시퀀스 다이어그램, 상태 전이도 및 실험 환경 배치도를 제안한다.
- 관련 연구와 참고문헌은 실제 학술 자료를 검색해 DOI 또는 공식 URL을 확인한 뒤 제시하도록 명시한다.
- 최종 원고 분량은 [목표 페이지 수]쪽 또는 [목표 글자 수]자로 작성한다.
- 투고 대상은 [학회/학술지/졸업논문]이며 인용 형식은 [IEEE/APA/학회 양식]을 따른다.

### 11. 사용자 제공 정보

아래 값은 작성 전에 사용자에게 확인하거나 빈칸으로 남겨라.

- 정확한 Jetson Nano 모델 및 메모리 용량: [입력 필요]
- JetPack 및 Ubuntu 버전: [입력 필요]
- 마이크 모델: [입력 필요]
- 스피커 모델: [입력 필요]
- Wi-Fi 동글 또는 네트워크 장치: [입력 필요]
- 전원 공급 장치: [입력 필요]
- 냉각 방식: [입력 필요]
- 백엔드 AI 모델 및 분석 방식: [입력 필요]
- 피험자 수와 음성 데이터 수집 방법: [입력 필요]
- 연구 윤리 승인 또는 사용자 동의 절차: [입력 필요]
- 실험 장소와 소음 측정 장비: [입력 필요]

먼저 논문 제목 후보 5개, 연구 기여점 3개, 전체 목차와 필요한 추가 정보 목록을 제시하라. 사용자가 승인하면 각 장을 순서대로 작성하라.

---

## 사용 팁

1. 먼저 프롬프트 마지막의 빈칸을 실제 하드웨어 정보로 채운다.
2. 한 번에 완성 논문을 요구하지 말고 목차, 시스템 설계, 실험 설계, 결과 분석 순서로 작성한다.
3. 실험 전에는 결과 장을 작성하지 않는다.
4. 논문에 들어갈 수치와 그래프는 실제 로그와 측정값을 기반으로 작성한다.
